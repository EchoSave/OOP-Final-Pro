package implementations;

import implementations.BSTree;

// PLACEHOLDER - belongs to Irteza Hassan. Delete on handoff.
// Real version reads/writes repository.ser.
public class RepositoryManager {
    public static BSTree<Word> load() { return new BSTree<>(); }
    public static void save(BSTree<Word> tree) { }
}